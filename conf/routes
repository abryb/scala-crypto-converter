# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /currencies/:code                 controllers.api.currency.GetCurrencyRatesController.apply(code: String, filter: List[String])
POST    /currencies/exchange              controllers.api.currency.CreateCurrencyExchangeOfferController.apply()
